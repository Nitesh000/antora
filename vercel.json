{
  "buildCommand": "pnpm --filter=v4 exec next build",
  "outputDirectory": "apps/v4/.next",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_AUTHOR_LOGIN\" = \"dependabot[bot]\" ]; then exit 0; else exit 1; fi"
}
